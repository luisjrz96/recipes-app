spring:
  application:
    name: recipes

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/recipes
  profiles:
    active: dev

server:
  port: 8087

